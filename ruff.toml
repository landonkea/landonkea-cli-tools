# Minimal lint config: classic Ruff defaults (pyflakes + basic pycodestyle errors).
# This repo has no other linter configured; this keeps CI focused on real bugs
# (unused imports, undefined names, syntax issues) rather than style opinions.
[lint]
select = ["E4", "E7", "E9", "F"]
